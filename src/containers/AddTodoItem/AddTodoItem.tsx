import React from 'react';
import { Grid } from '@mui/material';

export const AddTodoItem = () => {
  return (
    <Grid container display="flex" sx={{ width: '540px', height: '64px' }}>
      <Grid item>AddTodoItem</Grid>
    </Grid>
  );
};
